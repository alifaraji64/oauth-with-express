<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
		<link
			rel="stylesheet"
			href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
			integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
			crossorigin="anonymous"
		/>
		<title>Blogs</title>
		<style>
			body {
				justify-content: center;
				min-height: 100vh;
				align-items: center;
			}
			img {
				display: block;
				transform: scale(0.3);
			}
			nav {
				background: #ff5353;
				padding: 20px 10px;
			}
			nav ul {
				max-width: 960px;
				margin: 0 auto;
				padding: 0;
			}
			nav li {
				list-style-type: none;
				display: inline-block;
				margin: 0 10px 0 0;
			}
			nav a {
				color: #fff;
				font-size: 18px;
				opacity: 0.9;
				text-decoration: none;
				padding: 10px;
				display: block;
			}
			main {
				display: flex;
				justify-content: center;
				align-items: center;
			}
		</style>
	</head>
	<body>
		<nav>
			<ul>
				<li><a href="/">Homepage</a></li>
				<li><a href="/profile">Profile</a></li>
			</ul>
		</nav>
		<main>
			<%if(typeof blogs!='undefined'){%> <%blogs.forEach(function(blog){%>
			<div class="card" style="width: 18rem; margin: 2rem;">
				<img src="<%=blog.thumbnail%>" class="card-img-top" alt="profile-image" />
				<div class="card-body">
					<h2 class="card-title"><%=blog.title%></h2>
					<h5 class="card-text">
						<%=blog.blog.slice(0,20)%>
					</h5>
					<%if(googleId==blog.googleId){%>
					<a href="/edit-blog/<%=blog._id%>" class="btn btn-secondary">Edit Blog</a>
					<%}%>
				</div>
			</div>
			<% })%> <%}%>
		</main>
	</body>
</html>
